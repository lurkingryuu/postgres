name: Validate Libcedar Package Integration

on:
  workflow_dispatch:
    inputs:
      libcedar_version:
        description: "libcedar release tag to install (for example v0.1.0)"
        required: false
        default: "v0.1.0"
        type: string

jobs:
  validate-pg-cedar:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build PostgreSQL image against packaged libcedar
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./postgres-cedar.Dockerfile
          push: false
          load: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            LIBCEDAR_VERSION=${{ inputs.libcedar_version || 'v0.1.0' }}
